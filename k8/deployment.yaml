apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-angular
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-angular
  template:
    metadata:
      labels:
        app: app-angular
    spec:
      containers:
      - name: appangular
        image: haroldo89/demo-ingress-ang:8
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
          - name: http-port
            containerPort: 80