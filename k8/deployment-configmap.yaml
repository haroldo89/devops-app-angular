apiVersion: apps/v1
kind: Deployment
metadata:
  name: demoazure
spec:
  selector:
    matchLabels:
      app: demoazure
  replicas: 2
  template:
    metadata:
      labels:
        app: demoazure
    spec:
      containers:
        - name: demoazure
          image: haroldo89/nginx-env:1.2
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
            requests:
              memory: 256Mi
              cpu: "0.2"
          ports:
            - name: http-port
              containerPort: 80
          env:
          - name: nexus_user
            value: "admin"
          - name: nexus_password
            value: "Quevida890q12*"
          - name: ARTIFACT_URL
            value: "http://104.197.234.242:8081/repository/nodejs-release/taller/azure/74.zip"
          - name: FILE_NAME_ARTIFACT
            value: "74.zip"