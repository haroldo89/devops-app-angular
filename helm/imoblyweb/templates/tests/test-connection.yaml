apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imoblyweb.fullname" . }}-test-connection"
  labels:
    {{- include "imoblyweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "imoblyweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
