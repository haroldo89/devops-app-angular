apiVersion: v1
kind: Service
metadata:
  name: {{ include "imoblyweb.fullname" . }}
  labels:
    {{- include "imoblyweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imoblyweb.selectorLabels" . | nindent 4 }}
