apiVersion: v1
kind: Service
metadata:
  name: {{ include "basicdeploy.fullname" . }}
  labels:
    {{- include "basicdeploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basicdeploy.selectorLabels" . | nindent 4 }}
