apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demoazure.fullname" . }}-test-connection"
  labels:
    {{- include "demoazure.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demoazure.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
