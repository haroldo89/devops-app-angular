apiVersion: v1
kind: Service
metadata:
  name: {{ include "demoazure.fullname" . }}
  labels:
    {{- include "demoazure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demoazure.selectorLabels" . | nindent 4 }}
